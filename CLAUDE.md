# CLAUDE.md

Ynet Studio 是一个开源 AI 智能体开发平台。

- **后端**: Go + CloudWeGo/Hertz + DDD 架构
- **前端**: React + TypeScript + Rush.js monorepo (300+ packages)
- **API**: Thrift IDL 代码生成，前后端类型安全

## 快速参考

| 任务 | 详细文档 |
|------|---------|
| 新增/修改 API 接口 | 阅读 `docs/api-development-guide.md` |
| 开发规范、命令、架构 | 阅读 `docs/development-standards.md` |
| 可观测性 / Ynet Loop 集成 | 阅读 `docs/coze-loop-setup.md` |

## 核心开发命令

```bash
# 后端
make middleware          # 启动中间件
make server              # 启动后端 (localhost:8888)
make debug               # 完整环境

# 前端
rush install && rush build
cd frontend/apps/coze-studio && npm run dev  # localhost:3000

# API 代码生成
cd frontend/packages/arch/api-schema && npm run update   # 前端
cd backend && hz update -idl ../idl/xxx.thrift           # 后端

# 数据库
make sync_db             # 同步 schema
make dump_db             # 导出 schema
```

## 架构速览

### 后端 DDD 分层

```
backend/
├── api/           # HTTP 层（Handler 只做请求绑定，不写业务逻辑）
├── application/   # 应用服务层（业务流程编排）
├── domain/        # 领域层（核心业务逻辑）
├── infra/         # 基础设施层（数据库、外部服务）
├── crossdomain/   # 跨域服务
└── types/         # 共享类型（errno 错误码定义）
```

### 前端 Monorepo

```
frontend/
├── apps/coze-studio/      # 主应用
├── packages/arch/         # 核心架构（Level 1，不要改 bot-api）
├── packages/common/       # 共享工具（Level 2）
├── packages/workflow/     # 工作流（Level 3）
└── config/                # 共享配置
```

## 关键规则

1. **API 变更必须先改 IDL** → 生成前端代码 → 生成后端代码 → 实现业务逻辑
2. **不要修改** `@coze-arch/bot-api`，用 `@coze-studio/api-schema` 扩展
3. **不要手动修改**带有 `// Code generated by hz` 注释的文件
4. **Handler 不写业务逻辑**，调用 Application 层服务
5. **前端 API 导入用下划线**：`import { space_management } from '@coze-studio/api-schema'`
6. **大整数 ID** 在 IDL 中加 `api.js_conv='true'`，前端不要 `parseInt`
7. **Hz 生成前**检查 `backend/api/router/register.go` 的 `//INSERT_POINT` 格式（双斜杠后无空格）

## 环境要求

- Go >= 1.24
- Node.js >= 22
- pnpm 8.15.8
- Docker

## 关键依赖

- **Eino**: Agent 和 Workflow 运行时引擎
- **FlowGram**: 工作流前端画布编辑器
- **Hertz**: Go HTTP 框架
