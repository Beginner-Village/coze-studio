# 工作模式：代码开发

你当前处于**代码开发模式**，拥有完整的开发工具链访问权限。

## 可用工具
- **文件系统**：读取、编写、搜索项目中的所有文件
- **终端命令**：运行 shell 命令（构建、测试、lint、git 等）
- **代码搜索**：Grep/Glob 在整个代码库中搜索模式和文件
- **Git 操作**：提交、分支、查看 diff 和历史记录

## 开发规范
- 修改代码后必须运行 `typecheck` 和测试确认无报错
- 优先做 surgical edit（精准修改），不重构无关代码
- 不添加未修改代码的注释、类型注解或 docstring
- 不为不可能发生的场景添加错误处理
- 遵循项目现有的代码风格和目录结构

## 提交规范
- 使用 Conventional Commits 格式：`type(scope): description`
- 类型：feat / fix / chore / refactor / test / docs
- 提交前运行测试，不提交有失败测试的代码

## 调试流程
1. 先读取相关文件理解上下文
2. 用 Grep/Glob 搜索相关代码和引用
3. 做最小改动修复问题
4. 运行测试验证修复效果

## 生产部署：成都农商银行(CDRCB) 猎鹰智能体平台

本项目有一套部署在成都农商银行内网的生产环境。涉及部署、排障时，**必须先读取 memory 文件获取完整上下文**：

```
~/.claude/projects/-Users-luzhipeng-projects-ynet-coze-studio/memory/project_bank_deployment_status.md
```

该文件包含：服务器分工(209/210/211)、中间件地址矩阵、环境变量、已解决问题、遗留问题、踩坑记录。

### 关键约束
- **银行内网**：无法 SSH/SCP，脚本必须写到桌面(~/Desktop/)，用户通过 FDrive 跨网传输后手动执行
- **所有修复脚本**：写成完整的 .sh 放桌面，用户传到目标服务器 `/tmp/` 执行
- **不要假设能直接访问远程服务器**
