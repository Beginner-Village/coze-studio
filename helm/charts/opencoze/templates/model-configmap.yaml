apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openynet.fullname" . }}-model-config
  labels:
    {{- include "openynet.labels" . | nindent 4 }}
data:
{{- (.Files.Glob "files/conf/*").AsConfig | nindent 2 }}