{{- if and (eq (lower (default "rmq" .Values.cozeServer.env.YNET_MQ_TYPE)) "rmq") .Values.rocketmq.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openynet.fullname" . }}-rocketmq-broker
  labels:
    {{- include "openynet.labels" . | nindent 4 }}
spec:
  ports:
    - port: 10909
      targetPort: 10909
      name: broker-a
    - port: 10911
      targetPort: 10911
      name: broker-b
    - port: 10912
      targetPort: 10912
      name: broker-c
  selector:
    app.kubernetes.io/component: rocketmq-broker
    app.kubernetes.io/name: {{ include "openynet.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}