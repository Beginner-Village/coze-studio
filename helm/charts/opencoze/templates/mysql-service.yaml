apiVersion: v1
kind: Service
metadata:
  name: {{ include "openynet.fullname" . }}-mysql
  labels:
    {{- include "openynet.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.mysql.port }}
      targetPort: {{ .Values.mysql.targetPort }}
      name: mysql
  selector:
    app.kubernetes.io/component: mysql
    app.kubernetes.io/name: {{ include "openynet.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}